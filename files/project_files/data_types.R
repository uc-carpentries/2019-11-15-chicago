# Addition!
3 + 5

# Division!
12/7

# Assignment
(area_hectares <- 1.0)

area_hectares <- 2.5

area_acres <- 2.47 * area_hectares

area_hectares <- 3.0

# Exercise 1 : define variables "length" and "width" and then "area" in terms of them?
length <- 20
width <- 30
area <- length*width

# Vectors
hh_members <- c(3,4,6,10,7,2)
wall_types <- c("brick", "drywalls", "stone")
length(hh_members)
class(wall_types)
mixed_types <- c(1,"4", 3)

# subsetting routines
hh_members[hh_members > 5]
# add more to hh_members
hh_members <- c(hh_members, c(2,4,5,6,9,12))
summary(hh_members)






